@extends('admin.layout')
@section('title', 'Auditoria')
@section('content')
<div class="admin-head"><div><h1>Auditoria</h1><p>Registro das operações importantes do sistema.</p></div></div>
<form class="card" method="GET" style="display:flex;gap:12px;align-items:end;flex-wrap:wrap;margin-bottom:20px"><div class="field"><label for="action">Ação</label><input id="action" name="action" value="{{ $filters['action'] ?? '' }}" placeholder="Ex.: STOCK"></div><div class="field"><label for="from">De</label><input id="from" name="from" type="date" value="{{ $filters['from'] ?? '' }}"></div><div class="field"><label for="to">Até</label><input id="to" name="to" type="date" value="{{ $filters['to'] ?? '' }}"></div><button class="btn" type="submit">Filtrar</button></form>
<section class="table-wrap"><table><thead><tr><th>Data</th><th>Usuário</th><th>Ação</th><th>Registro</th><th>Detalhes</th><th>IP</th></tr></thead><tbody>@forelse ($logs as $log)<tr><td>{{ $log->created_at->format('d/m/Y H:i:s') }}</td><td>{{ $log->user?->name ?? 'Sistema' }}</td><td>{{ str($log->action)->replace('_',' ')->title() }}</td><td>{{ class_basename($log->entity_type) }} #{{ $log->entity_id }}</td><td><details><summary style="cursor:pointer;color:var(--wine)">Ver</summary><pre style="white-space:pre-wrap;font-size:11px">{{ json_encode(['antes' => $log->old_values, 'depois' => $log->new_values], JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE) }}</pre></details></td><td>{{ $log->ip_address ?? '—' }}</td></tr>@empty<tr><td colspan="6" class="muted">Nenhum registro encontrado.</td></tr>@endforelse</tbody></table></section><div style="margin-top:20px">{{ $logs->links() }}</div>
@endsection
