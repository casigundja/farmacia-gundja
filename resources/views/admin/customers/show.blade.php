@extends('admin.layout')
@section('title', 'Detalhes do cliente')
@section('content')
<div class="admin-head"><div><h1>{{ $customer->user->name }}</h1><p>Cliente desde {{ $customer->created_at->format('d/m/Y') }}</p></div><a class="btn btn-secondary" href="{{ route('admin.customers.index') }}">Voltar</a></div>
<div class="cards" style="grid-template-columns:repeat(3,1fr)"><div class="metric"><span>E-mail</span><strong style="font-size:18px">{{ $customer->user->email }}</strong></div><div class="metric"><span>Telefone</span><strong style="font-size:18px">{{ $customer->phone ?: '—' }}</strong></div><div class="metric"><span>CPF</span><strong style="font-size:18px">{{ $customer->cpf ?: '—' }}</strong></div></div>
<h2>Endereços</h2><div class="table-wrap" style="margin-bottom:24px"><table><thead><tr><th>Endereço</th><th>CEP</th><th></th></tr></thead><tbody>@forelse ($customer->addresses as $address)<tr><td>{{ $address->street }}, {{ $address->number }} · {{ $address->neighborhood }}, {{ $address->city }}/{{ $address->state }} @if ($address->is_default)<span class="status">Padrão</span>@endif</td><td>{{ $address->zipcode }}</td><td>{{ $address->complement }}</td></tr>@empty<tr><td colspan="3">Nenhum endereço salvo.</td></tr>@endforelse</tbody></table></div>
<h2>Pedidos</h2><div class="table-wrap"><table><thead><tr><th>Pedido</th><th>Data</th><th>Status</th><th>Pagamento</th><th>Total</th></tr></thead><tbody>@forelse ($customer->orders as $order)<tr><td>{{ $order->order_number }}</td><td>{{ $order->created_at->format('d/m/Y H:i') }}</td><td>{{ str($order->status)->replace('_', ' ')->title() }}</td><td>{{ $order->payments->map(fn ($payment) => str($payment->status)->title())->join(', ') ?: '—' }}</td><td>R$ {{ number_format((float) $order->total, 2, ',', '.') }}</td></tr>@empty<tr><td colspan="5">Nenhum pedido registrado.</td></tr>@endforelse</tbody></table></div>
@endsection
